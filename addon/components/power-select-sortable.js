import Ember from 'ember';
import layout from '../templates/components/power-select-sortable';

export default Ember.Component.extend({
  layout,
  tagName: '',
  triggerComponent: "power-select-sortable/trigger"
});
